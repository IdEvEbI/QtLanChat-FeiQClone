#pragma once

#include <string>

namespace LibrarySystem {
    namespace Utils {
        // 工具函数：格式化字符串
        std::string formatBookInfo(const std::string& title, const std::string& author);
    }
}

